# Webapp

Frontend shell for Never Forget.

### Local commands

```powershell
pnpm install
pnpm dev
pnpm lint
pnpm typecheck
pnpm test
pnpm build
```

### Current routes

- `/capture`
- `/review-queue`
- `/sessions`
- `/memory-overview`
