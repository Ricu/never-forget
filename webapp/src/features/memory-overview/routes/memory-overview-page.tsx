import { RoutePlaceholder } from "@/features/shared/route-placeholder"

export function MemoryOverviewPage() {
  return (
    <RoutePlaceholder
      eyebrow="Placeholder route"
      title="Memory Overview"
      summary="Reserved for browsing persisted memory output. No memory cards or read-side data ship in this slice."
    />
  )
}
