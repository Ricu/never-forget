import { RoutePlaceholder } from "@/features/shared/route-placeholder"

export function CapturePage() {
  return (
    <RoutePlaceholder
      eyebrow="Placeholder route"
      title="Capture"
      summary="Reserved for the primary capture surface. No input controls ship in this slice."
    />
  )
}
